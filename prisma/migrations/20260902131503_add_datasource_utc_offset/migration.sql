BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[DataSource] ADD [utcOffsetMinutes] INT NOT NULL CONSTRAINT [DataSource_utcOffsetMinutes_df] DEFAULT 300;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
